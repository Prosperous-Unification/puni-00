## Failure proofs

| Proof | Part | Check                                 | Fault injected                                                                   | Test                                                                                      | Observed failure                                                                                                                                                                                                             |
| ----- | ---- | ------------------------------------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1    | 1    | Unknown rule refusal                  | Return the first registered rule                                                 | `refuses an unregistered rule identifier and names every registered rule`                 | Transcribed by the planner from attempt `010-4-rule-model.part-1.20260919T234349Z`: the production CLI test saw `Expected: 1`, `Received: 0` for the exit status; replayed by the planner on 2026-09-20 with the same result |
| P2    | 2    | External policy containment           | Delete `assertExternal`                                                          | `refuses a rule policy inside the worktree even when the repository argument is interior` | Expected the inside-candidate diagnostic; received empty stderr.                                                                                                                                                             |
| P3    | 2    | Resolved worktree containment         | Pass the raw repository argument                                                 | `refuses a rule policy inside the worktree even when the repository argument is interior` | Expected the inside-candidate diagnostic; received empty stderr.                                                                                                                                                             |
| P4    | 2    | Mode coverage                         | Delete the final registered-rule coverage loop                                   | `refuses a policy that states no mode for a registered rule`                              | Expected `rule policy states no mode for INV-CLASSIFY`; received empty stderr.                                                                                                                                               |
| P5    | 2    | Registered policy rules               | Disable the unregistered-rule branch                                             | `refuses a policy that names an unregistered rule`                                        | Expected the `NO-SUCH-RULE` diagnostic; received empty stderr.                                                                                                                                                               |
| P6    | 2    | Unique policy rule identifiers        | Delete the duplicate branch                                                      | `refuses a policy that states a mode for one rule twice`                                  | Expected `rule policy states a mode for MOD-INDEX twice`; received empty stderr.                                                                                                                                             |
| P7    | 2    | Ratchet refusal                       | Delete the ratchet branch                                                        | `refuses ratchet until the adopted set exists`                                            | Expected the INV-CLASSIFY ratchet refusal; received empty stderr.                                                                                                                                                            |
| P8    | 2    | Required policy inputs                | Return immediately                                                               | `refuses a selected rule whose policy input is absent`                                    | Expected the missing `policy.classificationPolicy` diagnostic; received empty stderr while the verdict remained disallowed.                                                                                                  |
| P9    | 2    | Policy decoding boundaries            | Inject non-fatal UTF-8, unguarded JSON, and undeclared-key acceptance separately | `refuses malformed, non-UTF-8, unreadable, absent and undeclared-key policies distinctly` | UTF-8: received malformed-JSON instead of non-UTF-8; JSON: received bare `JSON Parse error: Expected '}'`; schema: expected `unexpected must be removed`, received empty stderr.                                             |
| P20   | 2    | Unusable policy read                  | Fall back to an empty policy after a read failure                                | `refuses malformed, non-UTF-8, unreadable, absent and undeclared-key policies distinctly` | Unreadable policy received `rule policy states no mode for INV-CLASSIFY` instead of `cannot open rule policy ...: EACCES`; the containment test also failed.                                                                 |
| P10   | 2    | Check flag validation                 | Accept any seventh argument                                                      | `refuses an unknown selection kind, an unknown flag and an unknown narrowed rule`         | Expected the `--only` diagnostic; received empty stderr and the narrowed check exited 0.                                                                                                                                     |
| P11   | 2    | Selection-kind refusal                | Treat every kind as committed                                                    | `refuses an unknown selection kind, an unknown flag and an unknown narrowed rule`         | Expected check usage; received `rule INV-CLASSIFY needs policy.classificationPolicy, which the rule policy omits`.                                                                                                           |
| P12   | 2    | Refused verdict exit status           | Delete `process.exitCode = 1`                                                    | `refuses an unindexed candidate in every mode and exits 1`                                | Expected exit 1; received exit 0.                                                                                                                                                                                            |
| P21   | 2    | Policy-aware explain mode             | Drop `mode` from the explanation                                                 | `prints the policy identifier and the stated mode`                                        | Deep equality failed with expected `mode: "enforce"` absent from the received record.                                                                                                                                        |
| P13   | 3    | Finding effect mapping                | Always return debt                                                               | `turns direct-entry debt into a refusal under enforce`                                    | Expected exit 1; received exit 0.                                                                                                                                                                                            |
| P14   | 3    | Unevaluated verdict disposition       | Delete the unevaluated condition                                                 | `refuses a rule whose prerequisite failed, in observe mode`                               | Expected exit 1; received exit 0.                                                                                                                                                                                            |
| P15   | 3    | Direct-entry prerequisite propagation | Return an empty observed list                                                    | `refuses a rule whose prerequisite failed, in observe mode`                               | Expected exit 1; received exit 0 with no finding and no unevaluated rule.                                                                                                                                                    |
| P16   | 3    | Direct-entry debt propagation         | Replace the mapped findings with an empty list                                   | `reports an index over the direct-entry limit with its exact counts`                      | Expected the exact 41-entry debt finding; received `[]`.                                                                                                                                                                     |
| P17   | 3    | Classification adapter invocation     | Skip classification                                                              | `refuses an unclassifiable entry in observe mode`                                         | Expected exit 1; received exit 0 with an empty unevaluated list.                                                                                                                                                             |
| P18   | 3    | Unresolved relationship propagation   | Replace the mapped findings with an empty list                                   | `reports a declared relationship that the candidate leaves unresolved`                    | Expected the `dynamic-shell-read` debt finding; received `[]`.                                                                                                                                                               |
| P19   | 3    | Wrapped failure classification        | Return an empty observed list from the catch                                     | `refuses an unclassifiable entry in observe mode`                                         | Expected exit 1; received exit 0 with an empty unevaluated list.                                                                                                                                                             |
| P22   | 3    | Default all-rules selection           | Select no rules when `--rule` is absent                                          | `allows the canonical candidate under every registered rule`                              | Expected all four registered identifiers; received `[]` while the verdict remained allowed.                                                                                                                                  |

Part 1's observed output is in its attempt's log directory outside this repository; the planner
transcribes it when committing.

## Commands and results

### Part 2

- Baseline rule suite: exit 0; `2 pass`, `0 fail`.
- Test-first rule suite: exit 1; `2 pass`, `11 fail`; `unknown command: check` and four-argument `explain` refusal observed.
- Post-implementation rule suite: exit 0; `13 pass`, `0 fail`.
- Candidate-reader and trusted-policy regression suites: exit 0; `56 pass`, `0 fail`, `Ran 56 tests across 2 files`.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:typecheck`: exit 0; `Successfully ran target typecheck for project twilight-bureaucrat`.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:lint:source`: the first run exited 1 because `check.ts` imports were unsorted; direct ESLint reported `simple-import-sort/imports`. After matching the dry-run import order, exit 0; `Successfully ran target lint:source for project twilight-bureaucrat`.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:build`: exit 0; `Successfully ran target build for project twilight-bureaucrat`.
- Owned-file `bunx prettier --write`: exit 0; all nine Part 2 files formatted.
- `NX_DAEMON=false bunx nx format:check --all`: exit 0; no output.
- Strict `OPENSPEC_TELEMETRY=0` validation: exit 0; one JSON report with `99` passed and `0` failed.
- Not run here, pending planner verification because they can require Git writes or staged files: whole `twilight-bureaucrat:test`, `twilight-bureaucrat:test:package`, and `tool-devsync:test` targets.
- `bin/h2puni-gate.sh` was not run because the host gate is unavailable on this machine.

### Part 3

- Baseline rule suite: exit 0; `13 pass`, `0 fail`, `Ran 13 tests across 1 file.`
- Post-addition rule suite: exit 0; `19 pass`, `0 fail`, `Ran 19 tests across 1 file.`
- P13 through P19 plus P22 named mutation runs: each exited 1 with `0 pass`, `18 filtered out`, `1 fail`, and `Ran 1 test across 1 file.`; each restored named run exited 0 with `1 pass`, `18 filtered out`, and `0 fail`.
- Final focused rule suite: exit 0; `19 pass`, `0 fail`, `Ran 19 tests across 1 file.`
- Relationship and index regression suites: exit 0; `76 pass`, `0 fail`, `Ran 76 tests across 2 files.`
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:typecheck`: exit 0; `Successfully ran target typecheck for project twilight-bureaucrat`.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:lint:source`: exit 0; `Successfully ran target lint:source for project twilight-bureaucrat`.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:build`: exit 0; `Successfully ran target build for project twilight-bureaucrat`.
- Owned-file `bunx prettier --write`: exit 0; all six Part 3 files formatted.
- `NX_DAEMON=false bunx nx format:check --all`: exit 0; no output.
- Strict `OPENSPEC_TELEMETRY=0` validation: exit 0; one JSON report with `99` passed and `0` failed.
- Not run here, pending planner verification because they can require Git writes or staged files: whole `twilight-bureaucrat:test`, `twilight-bureaucrat:test:package`, and `tool-devsync:test` targets.
- `bin/h2puni-gate.sh` was not run because the host gate is unavailable on this machine.

### Part 4

- Baseline rule suite: exit 0; `19 pass`, `0 fail`, `Ran 19 tests across 1 file.`
- Focused packaging suite: exit 0; `2 pass`, `0 fail`, `Ran 2 tests across 1 file.`
- Combined rule and packaging suites: exit 0; `21 pass`, `0 fail`, `Ran 21 tests across 2 files.` The rule count remained 19 and the packaging count remained 2.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:typecheck`: exit 0; `Successfully ran target typecheck for project twilight-bureaucrat`.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:lint:source`: exit 0; `Successfully ran target lint:source for project twilight-bureaucrat`.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:build`: exit 0; `Successfully ran target build for project twilight-bureaucrat`; `dist/bin.mjs` and `dist/toolkit/validator.mjs` were rebuilt.
- Owned-file `bunx prettier --write`: exit 0; all four Part 4 files were unchanged.
- `NX_DAEMON=false bunx nx format:check --all`: exit 0; no output.
- Strict `OPENSPEC_TELEMETRY=0` validation: exit 0; one JSON report with `99` passed and `0` failed.
- Not run here, pending planner verification because they can require Git writes or staged files: whole `twilight-bureaucrat:test`, `twilight-bureaucrat:test:package`, and `tool-devsync:test` targets.
- `twilight-bureaucrat:pack` was not run because it depends on `build` and duplicates the built-executable coverage; the planner runs it if packaging output changes.
- `bin/h2puni-gate.sh` was not run because the host gate is unavailable on this machine.
