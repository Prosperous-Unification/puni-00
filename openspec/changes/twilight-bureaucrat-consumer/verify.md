# Verification

## Commands and results

All runs 2026-09-18 on the `change/tbf-p5` worktree, not h2puni. The h2puni full gate was not run (this host is not h2puni); no CI run exists for these commits.

- `bunx nx run twilight-bureaucrat:test:package --skip-nx-cache` at `9ce66b7e53028098184058d94a035616a25850e2`: exit 0, 44 pass, 0 fail, 268 assertions across 6 files in 94.4s, including the 21 cases of `src/packaging/consumer-bootstrap.test.ts`. The target rebuilt and packed at that commit: 161 files, SHA-256 `2aadf267c477b5835e6e34498161a5210b7d5e1b9d45a1c30059563b68d4ed83`, npm shasum `6c7d370e57a4fd6b4f91f06469c74214e7e16444`, integrity `sha512-al4wMt193YDuqtMWyiaOwbZlut1eLD3N7ROMm4Ff++Hza/hvY3mm+uk16sV192XQbBKRRnyrebpZHkJzPnfaRA==`. The consumer matrix served exactly that file from an in-process loopback registry and asserted the bootstrap output, the installed lock and `admission.json` all carry the served integrity.
- Consumer matrix assertions: the committed `consumer.json` selects `archive-launcher` and installs nothing; the installed package certifies the reviewed fixture revision (`"certified":true`, exit 0) through the workflow's own `Bootstrap trusted package` and `Verify exact candidate with external trust` run blocks, with an activation prepared by the installed `prepare-activation` from fixture review evidence; seven hostile candidates (package pin, lockfile, `.npmrc`+`bunfig.toml`, lifecycle scripts with `trustedDependencies`, validator source, Nx plugin, activation variables plus edited `bin/tool-wiki-lint.sh`) each ran with hostile install inputs also at the workspace root and hostile `BUN_OPTIONS`/registry variables inherited: bootstrap kept the base integrity, the hostile registry received zero requests, no sentinel file existed, admission exited nonzero without certification and wrote no `admission.json`. Unselected route, missing lock, absent/unreadable/malformed switch, scripted base manifest, foreign-registry lock, reused scratch, incompatible and legacy activations and an unconfigured activation root each exit 78 by name. The root route reports `inactive` without an activation, certifies with one, and refuses a missing/range pin and re-entry.
- `bunx nx run twilight-bureaucrat:test --skip-nx-cache` (pre-commit tree of `66032a7b`): exit 1, 635 pass, 20 fail, run concurrently with other suites. Baseline at `8d98168d` with P5 stashed, same four files (`root-migration`, `pilot-policy`, `relocation-activation`, `trusted-policy`): 13 fail, all 13 also failing in the P5 run (`infra/ansible/ansible.cfg matched 0 classification rules`, absent fixture `nx.json`, skip probe). The 7 extra were rerun alone with P5 applied: `root-migration.test.ts` + `trusted-policy.test.ts` gave 85 pass, 2 fail, both in the baseline set. No failure is attributed to P5; the pre-existing ones belong to the base branch.
- `bunx nx run tool-devsync:test --skip-nx-cache`: exit 1, 202 pass, 12 fail; baseline at `8d98168d` with P5 stashed: 196 pass, 12 fail, identical failing names (fleet `@infra` alias, `apps/wiki/cli/fixtures/consumer` project, fleet plan exemptions, `tool-fleet` inputs). The 6 new `bureaucrat-consumer.test.ts` cases pass, including shellcheck of both bootstrap scripts and the route.
- `twilight-bureaucrat:lint:source`, `twilight-bureaucrat:typecheck`, `tool-devsync:lint`, `tool-devsync:typecheck` (all `--skip-nx-cache`): exit 0.
- `bash bin/h2puni-gate.test.sh`: exit 0, `all cases passed`. Gate scripts are unchanged; the source exclusion already names `twilight-bureaucrat`.
- `bunx @fission-ai/openspec@1.12.0 validate --all --json | jq -e '.summary.totals.failed == 0'`: `true`. `bunx nx format:check --all`: exit 0.
- `actionlint` is not installed; no actionlint result is claimed. Nothing was published; no registry, host or secret was touched.

## Failure proofs

Each fault was injected into the named production file, the named test was watched failing, and the file was restored byte-for-byte before the green run above.

| Check                            | Injected fault                                                                             | Production-path test                                     | Observed result                                                                                                             |
| -------------------------------- | ------------------------------------------------------------------------------------------ | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Base-owned pin                   | `bootstrap.sh` read `bun.lock` from the step cwd and dropped the registry-prefix condition | hostile matrix through `Bootstrap trusted package`       | Candidate lock installed the hostile tarball; its executable ran; every case failed (exit 78 at version check instead of 0) |
| Lock registry                    | Registry-prefix condition removed                                                          | foreign-registry lock                                    | Hostile tarball installed; refusal came only from the later version check, not the lock check                               |
| Lock presence                    | Lock refusals removed                                                                      | installed-package without lock                           | Frozen install resolved from the registry with no lock and exited 0                                                         |
| Base manifest shape              | Manifest validation replaced by a plain `jq` read                                          | base manifest with `preinstall` sentinel                 | Exit 0 instead of 78                                                                                                        |
| Disabled lifecycle scripts       | Manifest validation removed and `--ignore-scripts` dropped                                 | same                                                     | Base `preinstall` sentinel written                                                                                          |
| Bootstrap environment            | `env -i` replaced by `env` in `trusted_bun`                                                | hostile `BUN_OPTIONS` preload                            | Preload sentinel written                                                                                                    |
| Switch validation                | Field checks removed from the `consumer.json` parse                                        | unknown admission value                                  | Fell through to the package route, exit 0 instead of 78                                                                     |
| Scratch reuse                    | Existing-scratch refusal removed, child mkdir tolerant                                     | second bootstrap into the same scratch                   | Exit 0 instead of 78                                                                                                        |
| Package/activation compatibility | `admit.sh` toolkit-release comparison forced false                                         | activation whose `toolkit-release` names another toolkit | Candidate certified at exit 0                                                                                               |
| Admission environment            | `env -i` removed from the installed `lint` invocation                                      | certified run with hostile `BUN_OPTIONS`                 | Preload sentinel written                                                                                                    |
| Route selection                  | Workflow `*)` branch exited 0                                                              | verify block with no route output                        | Green admission instead of 78                                                                                               |
| Bootstrap ordering               | Candidate checkout moved ahead of the base checkout and bootstrap                          | workflow step-order test                                 | Failed: bootstrap index 6 not below candidate index 4                                                                       |
| Route recursion                  | Re-entry guard forced false                                                                | package lint that calls the route again                  | Exit 0 after the fixture depth bound instead of 70                                                                          |
| Deployment identity binding      | Activation, then package, shape check removed from `parseTrustedAdmission`                 | `main` activation version; sha1 package integrity        | Each returned a descriptor instead of throwing                                                                              |
| Deployment source binding        | Source comparison removed                                                                  | admission for `aaaa…` deploying `9999…`                  | Descriptor returned                                                                                                         |
| Staged image identity            | `sha256:` digest check removed                                                             | `:latest` image reference                                | Descriptor returned                                                                                                         |

Defense in depth, labelled: removing only `--ignore-scripts` cannot fail a test, because the manifest check already forbids `scripts` and `trustedDependencies`; the lifecycle row therefore records the combined fault. A missing-activation record is also rejected by the exact-key check, so the activation row uses a present-but-malformed identity.
