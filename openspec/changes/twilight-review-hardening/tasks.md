# Twilight review hardening tasks

Scope: apply the accepted branch review, using [design](design.md) and the
[workflow contract](specs/twilight/workflow-maintenance/spec.md). Runtime changes
are specification corrections only. The existing product plan remains unexecuted.

## Task 1: Resolve the architecture and planning contracts

- [x] 1.1 Amend proposed design/specs/tasks, glossary and explanatory docs for
      restore compatibility, live revocation, dispatch fencing, idempotent human
      decisions, parallel plans and input/output receipt lineage.

Owns `docs/twilight-structure/{CONTEXT,assumptions,client-repositories,product-experience}.md`
and `openspec/changes/twilight-control-plane/{design,tasks,specs/**}.md`.
Preserve established terms and historical receipts. Add concrete failure scenarios
and task-level oracles without claiming they ran. Review cross-document agreement,
relative links, OpenSpec structure and formatting. Does not depend on Task 2.

## Task 2: Repair and reproduce the repository workflow

- [x] 2.1 Include conditional design in fast-forward traversal; make archive
      failures explicit; generate installed variants from canonical sources and
      enforce reproducibility in the Nx/CI gate.

Owns `openspec/schemas/twilight-v1/schema.yaml`, generated `.agents/.claude`
OpenSpec skills and commands, scoped workflow tooling/targets/tests and upgrade
documentation. First retain a negative for omitted design traversal and workflow
drift. Observe failure, implement, observe restoration; exercise the actual pinned
CLI and generator entrypoints. Test real source absence/unreadability and a
deliberately divergent provider variant. Record the observed failures before Proof
comments. Skill scenarios cover malformed output and controlled optional absence.

## Task 3: Verify the combined change

- [x] 3.1 Review both slices together, run affected tests/lint/typechecks and the
      repository format/OpenSpec/doc-cap checks, and write `verify.md` with observed
      failure proofs, limits and the disposition of every original finding.

Depends on Tasks 1–2. Owns this change's evidence and navigation to the corrections.
No runtime implementation, remote writes, merge, release or archive is authorized
by completing this task. Use disposable fixtures for mutations, preserving the
working branch and other agents' write scopes.

## Task 4: Close the second review round on the workflow contract

- [x] 4.1 Make `design.md` a present artifact with an applicability section rather
      than a prose contradiction, and say where `verify.md` is required.

Owns `openspec/schemas/twilight-v1/{schema.yaml,templates/*.md}`, `openspec/config.yaml`,
this change's specs, and `openspec/changes/twilight-sdlc-pilot/specs/twilight/{sdlc,knowledge}`.
The schema instruction, the templates, this change's `workflow-maintenance` spec and
the pilot's `sdlc` spec now say one thing. Tests: `cli.integration.test.ts`'s
'the pinned CLI blocks planning while design.md is absent' and 'an applicability-only
design.md unblocks planning, and apply needs no verify.md', both against the pinned
1.12.0 CLI. Negatives: `tasks.requires` loses `design`, observed on
`Expected: "blocked" / Received: "ready"` (`evidence/design-traversal-red.txt`);
`apply.requires` gains `verify`, observed on `Expected: "ready" / Received: "blocked"`
(`evidence/apply-verify-red.txt`). `openspec/config.yaml`'s global `rules.design` is
rewritten schema-neutrally because a probe showed config rules are not schema-scoped
(`evidence/config-rules-are-global.txt`); it has no automated negative and is a
documented probe, not a check.

- [x] 4.2 Close the generator's blind spots and its duplicated configuration.

Owns `tools/tool-workflows/**`. Four checks, each with its named negative:
an added file under a generated output root — 'an added file under a generated
output root is refused' (three paths); fault: the scan replaced by `void ownedRoots;`,
observed on `Expected: 1 / Received: 0` (`evidence/untracked-output-red.txt`).
A generated copy naming its source — 'a generated copy names the file a human should
edit instead'; faults: `markGenerated` returning its input, observed on
`Expected to contain: "<!-- Generated by tool-workflows from …"`, and the marker
strip removed from `readSource`, observed on `Expected: 0 / Received: 1` at the
re-check (`evidence/generated-marker-red.txt`). Prettier options read from
`.prettierrc.json` — 'the repository prettier configuration decides the generated
formatting' plus absent/unreadable/malformed cases; faults: hardcoded options,
observed on `Expected: 1 / Received: 0`, and the object guard deleted, observed on
`Expected to contain: ".prettierrc.json"` (`evidence/prettier-config-red.txt`).
Exactly one `## Command Template` heading — 'a duplicated command template heading
stops generation before writes'; fault: the count branch deleted, observed on
`Expected: 1 / Received: 0` (`evidence/command-template-duplicate-red.txt`).
`mode` is a `'--check' | '--write'` union validated at the argv boundary; fault: a
literal `'--bogus'` passed to `generateWorkflows`, observed as
`error TS2345: Argument of type '"--bogus"' is not assignable to parameter of type
'Mode'` (`evidence/mode-union-red.txt`). The chmod-000 cases now assert
`process.getuid() !== 0` first; fault: `process.getuid` preloaded to return 0,
observed on `error: Running as root: chmod 000 still reads, so this fault test
cannot fail` (`evidence/root-guard-red.txt`). Real uid 0 was not exercised.

- [x] 4.3 Put the pinned-CLI proof inside the merge gate and correct the claims
      that had no artifact behind them.

Owns `.github/workflows/ci.yml`, `docs/2026-09-06-openspec-upgrade.md` and this
change's `verify.md`. CI gains an `OpenSpec schema graph (pinned CLI)` step that
installs `@fission-ai/openspec@1.12.0` into a scratch directory and runs
`tool-workflows:integration` with `OPENSPEC_CLI` set; the step body was executed
locally (`evidence/ci-integration-step.txt`) but the step itself has never run on a
runner. The upgrade document records the measured skill/command divergence
(`evidence/skill-command-divergence.txt`) instead of the earlier parity claim, which
had compared renamed invocations. `verify.md` is restructured onto the sdd-lean
verify sections, and every row that asserted a count or an exit now links a
retained artifact.

## Task 5: Resolve resource-control and configuration gaps

- [x] 5.1 Amend the proposed configuration, specs, design, glossary and delivery
      tasks for cumulative budgets/clocks, field-specific overrides, profile epochs,
      comparable observations, explicit stage ordering and organization pricing.
- [x] 5.2 Check the amended documents and configuration together, run the relevant
      repository checks and record the six findings' dispositions and limits.

Owns the current Twilight proposal and `execution.yaml`; preserves historical
reviews and leaves every product implementation task unchecked. Use the accepted
review's concrete counterexamples: spend/holds across a profile change, concurrent
agent time versus wall time, skipped observations versus zero findings, mixed-profile
recovery, unordered shared artifacts, and inconsistent optional activities. Record
future runtime negatives in the delivery tasks. This slice changes proposed
contracts, so temporary configuration probes are documentation evidence rather
than a claim that a production compiler, budget gate or evaluator exists.
