## Failure proofs

| Proof | Slice | Check                             | Fault injected                                      | Test                                                                              | Observed failure                                                                                                                                                                                      |
| ----- | ----- | --------------------------------- | --------------------------------------------------- | --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1    | 1     | Unknown template refusal          | Return the first registered template                | `refuses an unregistered template identifier and names every registered template` | Exit 1; `Expected: 1`, `Received: 0`; exactly one test ran and failed. Restored bytes matched with `cmp`, then the named test passed.                                                                 |
| P2    | 3     | Candidate-relative subject guard  | Delete the guard                                    | `refuses a subject that is not candidate-relative`                                | Exit 1; expected the candidate-relative refusal, received `subject selects no candidate file: /etc/passwd`; exactly one test ran and failed (`P2.log`).                                               |
| P3    | 3     | Empty-selection refusal           | Return a finding-free verification                  | `refuses a subject that selects nothing`                                          | Exit 1; `Expected: 1`, `Received: 0`; exactly one test ran and failed (`P3.log`).                                                                                                                     |
| P4    | 3     | Subject-kind refusal              | Set `mistaken` to false                             | `refuses a file template pointed at a directory`                                  | Exit 1; expected the file-template subject-kind refusal, received `subject selects no candidate file: src/modules/widget`; exactly one test ran and failed (`P4.log`).                                |
| P5    | 3     | Refused-verification exit status  | Delete the exit-code assignment                     | `reports a file whose name lacks the kind suffix`                                 | Exit 1; `Expected: 1`, `Received: 0`; exactly one test ran and failed (`P5.log`).                                                                                                                     |
| P6    | 3     | UTF-8 boundary                    | Drop fatal decoding                                 | `refuses a candidate file that is not UTF-8`                                      | Exit 1; expected the non-UTF-8 refusal and received empty stderr; exactly one test ran and failed (`P6.log`).                                                                                         |
| P7    | 3     | Import-scan boundary              | Return no imports after a scan failure              | `refuses a file that does not parse`                                              | Exit 1; expected the import-scan refusal and received empty stderr; the finding still made the command exit 1; exactly one test ran and failed (`P7.log`).                                            |
| P8    | 3     | Name-suffix constraint            | Return no findings                                  | `reports a file whose name lacks the kind suffix`                                 | Exit 1; `toEqual` lost the `feature.suffix` finding; exactly one test ran and failed (`P8.log`).                                                                                                      |
| P9    | 3     | One-kind-per-file constraint      | Require more than two kinds                         | `reports a standalone file whose name declares two kinds`                         | Exit 1; `toEqual` lost the `resource.one-kind` finding; exactly one test ran and failed (`P9.log`).                                                                                                   |
| P10   | 3     | Declares-one constraint           | Accept fewer than 99 declarations                   | `reports a service that states two declaration tags`                              | Exit 1; `Expected: 1`, `Received: 0`; exactly one test ran and failed (`P10.log`).                                                                                                                    |
| P11   | 3     | Imports-no-kind constraint        | Check an empty kind list                            | `reports a side-effect import of a repository and ignores comments and strings`   | Exit 1; `Expected: 1`, `Received: 0`; exactly one test ran and failed (`P11.log`).                                                                                                                    |
| P12   | 3     | Sibling-test constraint           | Return no findings                                  | `reports a repository adapter with no sibling test`                               | Exit 1; `Expected: 1`, `Received: 0`; exactly one test ran and failed (`P12.log`).                                                                                                                    |
| P13   | 5     | Scope refusal                     | Return no findings from the refusal                 | `refuses a requirement whose constraint no file artifact can satisfy`             |                                                                                                                                                                                                       |
| P14   | 3     | Requirement-driven evaluation     | Skip the first requirement                          | `evaluates exactly the requirements the template states`                          | Exit 1; `toEqual` lost the sole `probe.tag` finding; exactly one test ran and failed (`P14.log`).                                                                                                     |
| P15   | 5     | Required-file constraint          | Return no findings                                  | `reports a module with no index, no contract, no kind file and no test`           |                                                                                                                                                                                                       |
| P16   | 5     | Index-sections constraint         | Treat no section as absent                          | `reports an index that omits one of its sections`                                 |                                                                                                                                                                                                       |
| P17   | 5     | Kind-file-present constraint      | Return no findings                                  | `reports a module with no index, no contract, no kind file and no test`           |                                                                                                                                                                                                       |
| P18   | 5     | Test-present constraint           | Return no findings                                  | `reports a module with no index, no contract, no kind file and no test`           |                                                                                                                                                                                                       |
| P19   | 5     | Files-stay-in-module constraint   | Allow one more directory level                      | `reports a file that sits below the module directory`                             |                                                                                                                                                                                                       |
| P20   | 5     | Module one-kind wiring            | Pass no files to the one-kind handler               | `reports a module file that declares two kinds`                                   |                                                                                                                                                                                                       |
| P21   | 5     | Kind-template delegation          | Drop all kind files before delegation               | `reports a kind file inside a module that breaks its own template`                |                                                                                                                                                                                                       |
| P22   | 3     | Unknown-action refusal            | Delete the final usage throw                        | `refuses an unknown template action`                                              | Exit 1; `Expected: 1`, `Received: 0`; exactly one test ran and failed (`P22.log`).                                                                                                                    |
| P23   | 3     | Selection-kind refusal            | Treat an invalid kind as committed                  | `refuses an unknown candidate selection kind`                                     | Exit 1; `Expected: 1`, `Received: 0`; exactly one test ran and failed (`P23.log`).                                                                                                                    |
| P24   | 3     | Line-comment declaration scanning | Scan raw source with a multiline regular expression | `counts a declaration tag only when it is a real line comment`                    | Exit 1; `toEqual` changed the message from `file states 0 @capability tags, expected exactly 1` to `file states 2 @capability tags, expected exactly 1`; exactly one test ran and failed (`P24.log`). |
| P25   | 5     | Whole-file parse boundary         | Delete the TypeScript parse in artifact decoding    | `refuses a module whose contract does not parse`                                  |                                                                                                                                                                                                       |

## Commands and results

### Slice 1

- Preparation rule suite: exit 0; `19 pass`, `0 fail`, `Ran 19 tests across 1 file.`
- Slice 1 absence checks: exit 0; no output, so `src/templates` and the `template` route were absent.
- Initial strict OpenSpec validation: exit 0; `105` passed and `0` failed. The report is retained under the attempt's evidence directory.
- Test-first template and packaging files: exit 1; `1 pass`, `5 fail`, `Ran 6 tests across 2 files.` The template cases reported `unknown command: template`; the packaging case reported `Expected to contain: "twilight-bureaucrat template <list|show"`.
- Post-implementation template and packaging files: exit 0; `6 pass`, `0 fail`, `Ran 6 tests across 2 files.` Four passes belong to the template file and two to the packaging file.
- P1 injected typecheck: exit 0; `Successfully ran target typecheck for project twilight-bureaucrat`.
- P1 named fault run: exit 1; `0 pass`, `3 filtered out`, `1 fail`, `Ran 1 test across 1 file.` The decisive assertion was `Expected: 1`, `Received: 0`.
- P1 restored named run: exit 0; `1 pass`, `3 filtered out`, `0 fail`, `Ran 1 test across 1 file.`
- Owned-file `bunx prettier --write`: exit 0; all twelve slice 1 paths formatted, with only `verify.md` and `build.test.ts` rewritten.
- Final template and packaging files: exit 0; `6 pass`, `0 fail`, `Ran 6 tests across 2 files.` Four passes belong to the template file and two to the packaging file.
- Final rule suite: exit 0; `19 pass`, `0 fail`, `Ran 19 tests across 1 file.` The baseline count remained unchanged.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:typecheck`: exit 0; `Successfully ran target typecheck for project twilight-bureaucrat`.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:lint:source`: exit 0; `Successfully ran target lint:source for project twilight-bureaucrat`; no warnings.
- `NX_DAEMON=false bunx nx format:check --all`: exit 0; no diagnostic output.
- Final strict `OPENSPEC_TELEMETRY=0` validation: exit 0; `105` passed and `0` failed. The JSON report is retained under the attempt's evidence directory.
- `git diff --check` and the section 1.9 ownership comparison: exit 0; exactly the twelve authorized paths were present.
- Not run here, pending planner verification because the whole targets may require Git writes or staged files: `twilight-bureaucrat:test`, `twilight-bureaucrat:test:package`, and `tool-devsync:test`.
- `bin/h2puni-gate.sh` was not run because the host gate is unavailable on this machine.

Planner, after slice 1, 2026-09-20: replayed P1 outside the sandbox (`selectTemplate` returning the first registered template for an unknown identifier): `refuses an unregistered template identifier and names every registered template` failed with `Expected: 1`, `Received: 0`, 3 pass and 1 fail; restored byte for byte, then 4 pass. The whole `twilight-bureaucrat` `test`, `test:package` (the packaged executable answers `--help` with the template command and `template list`), `lint:source` and `typecheck` succeeded; `tool-devsync:test` succeeded.

### Slice 2

- Preparation rule suite: exit 0; `19 pass`, `0 fail`, `Ran 19 tests across 1 file.`
- Template and candidate-reader baseline: exit 0; `5 pass`, `0 fail`, `Ran 5 tests across 2 files.` Four passes belonged to `templates.test.ts`; one belonged to `read-candidate.test.ts`.
- Test-first template file: exit 1; `5 pass`, `17 fail`, `Ran 22 tests across 1 file.` The CLI case `allows a repository adapter that names its port and has its sibling test` expected exit 0, received exit 1, and printed the template usage refusal. The pure case `evaluates exactly the requirements the template states` failed with `TypeError: verifyArtifact is not a function`.
- Post-implementation template file: exit 0; `22 pass`, `0 fail`, `Ran 22 tests across 1 file.`
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:typecheck`: exit 0; `Successfully ran target typecheck for project twilight-bureaucrat`.
- Final template and candidate-reader files: exit 0; `23 pass`, `0 fail`, `Ran 23 tests across 2 files.` Twenty-two passes belonged to `templates.test.ts`; the candidate-reader baseline remained one pass.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:lint:source`: exit 0; `Successfully ran target lint:source for project twilight-bureaucrat`; no lint diagnostics.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:build`: exit 0; `Successfully ran target build for project twilight-bureaucrat`.
- Owned-file `bunx prettier --write`: exit 0; all five slice 2 paths were unchanged.
- `NX_DAEMON=false bunx nx format:check --all`: exit 0; no format diagnostics.
- Strict `OPENSPEC_TELEMETRY=0` validation: exit 0; `105` passed and `0` failed. The JSON report is retained under the attempt's evidence directory.
- Negative proofs P2-P12, P14, and P22-P24 were not run because they belong to slice 3.
- Not run here, pending planner verification because the whole targets may require Git writes or staged files: `twilight-bureaucrat:test`, `twilight-bureaucrat:test:package`, and `tool-devsync:test`.
- `bin/h2puni-gate.sh` was not run because the host gate is unavailable on this machine.

Planner, after slice 2, 2026-09-20: the focused templates file passes 22 of 22 (slice 1's four plus eighteen). The whole `twilight-bureaucrat` `test`, `test:package`, `lint:source` and `typecheck` succeeded; `tool-devsync:test` succeeded; format check clean. Slice 2 injects no fault of its own: its checks get their negatives in slice 3.

### Slice 3

- Preparation rule suite: exit 0; `19 pass`, `0 fail`, `Ran 19 tests across 1 file.`
- Preparation template suite: exit 0; `22 pass`, `0 fail`, `Ran 22 tests across 1 file.`
- P2-P12, P14, and P22-P24 mutation type checks: each exited 0; each log records `status=0` (`P2-typecheck.log` through `P24-typecheck.log` for the slice 3 proof identifiers).
- P2-P12, P14, and P22-P24 named fault runs: each exited 1; each selected exactly one test, reported `1 fail`, and recorded the row-specific diagnostic above (`P2.log` through `P24.log` for the slice 3 proof identifiers).
- P2-P12, P14, and P22-P24 restorations: each restored copy matched with `cmp`; each complete focused rerun exited 0 with `22 pass`, `0 fail`, `Ran 22 tests across 1 file` (`P2-green.log` through `P24-green.log` for the slice 3 proof identifiers).
- Final focused template suite after the proof comments: exit 0; `22 pass`, `0 fail`, `Ran 22 tests across 1 file.`
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:typecheck`: exit 0; `Successfully ran target typecheck for project twilight-bureaucrat`.
- `NX_DAEMON=false bunx nx run twilight-bureaucrat:lint:source`: exit 0; `Successfully ran target lint:source for project twilight-bureaucrat`; no warnings.
- Owned-file `bunx prettier --write`: exit 0; formatted the four slice 3 paths.
- `NX_DAEMON=false bunx nx format:check --all`: exit 0; no diagnostic output.
- Strict `OPENSPEC_TELEMETRY=0` validation: exit 0; `105` passed and `0` failed. The JSON report is retained as `openspec-validation-slice3-initial.JVdVvM.json` in the attempt evidence directory.
- Not run here, pending planner verification because whole targets may require Git writes or staged files: `twilight-bureaucrat:test`, `twilight-bureaucrat:test:package`, and `tool-devsync:test`.
- `bin/h2puni-gate.sh` was not run because the host gate is unavailable on this machine.

Planner, after slice 3, 2026-09-20: slice 3 changed only `Proof:` comments (and one expression Prettier re-wrapped around a comment). Replayed P7 outside the sandbox (the import scan's catch returning `[]` instead of rethrowing with context): `refuses a file that does not parse` failed with `Expected to contain: "cannot scan the imports of src/modules/widget/widget.feature.ts"` against `Received: ""`, 21 pass and 1 fail; restored byte for byte, then 22 pass. Source lint, typecheck and the format check succeed.
