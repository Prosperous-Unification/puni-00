# 050.7 frontend lifetime map

Read-only planning against integration `6962f0cd2fae031229a107c19543926bb387bb42` on 2026-09-21, with the same frontend/design bytes independently checked through `78f14391c977b2adaf1d2d1f38e1d8154d98ad42`. This maps the accepted architecture in `docs/superpowers/specs/2026-09-19-code-organization-design.md`; it does not choose the pending runtime identifier grammar.

## Authority and DI Bag facts

The accepted design fixes three runtimes and their boundaries: application = page, session = signed-in user identity, project = selected project. Each is one DI Bag runtime; browser registrations use the explicit sync/async factory helpers; React receives narrow service contexts and never a bag.

The installed package is `di-bag@0.4.0`. Its shipped `README.md` and `dist/di-bag.d.ts` establish the usable semantics:

- browser code imports `di-bag`, not `di-bag/node`, and must use `DiBag.fromSyncFactory` / `DiBag.fromAsyncFactory` because automatic acquisition classification is unavailable in browsers;
- `build()` is lazy, so composition must resolve/start the services required for the lifetime rather than treating construction as startup;
- `withDisposal` transfers ownership explicitly; a `close()` method on a returned object does not do so by itself;
- repeated `close()` calls share shutdown, and cleanup runs dependents before dependencies;
- bounded `close({ timeoutMs, signal })` only bounds the wait. Cleanup continues after timeout/abort and can reject with pending disposer names or an aggregate cleanup failure;
- a parent closes tracked scopes, but not forks. The three accepted runtimes are separate lifetime owners, so their cascade and ordering remain application code responsibility unless the implementation deliberately uses tracked scopes.

The npm package omits the React guide and example owner, but the official `v0.4.0` tag contains a tested guide/recipe. That owner is copyable application source, not exported DI Bag API. Its generation fencing, stale-release and withdraw-before-retire mechanics are useful; its policy is incompatible with repository R5 because it starts a replacement after cleanup rejection or close-wait expiry. Puni must implement a local owner with the stricter fail-closed transition gate below rather than copy the tagged owner unchanged.

## Required implementation order

The page-level project runtime is **not constructible from today's services**. `planFeedForReader` still requires React delivery callbacks (`publish`, `setConnected`, `announceRefusal`) and a broad `ProjectApi`. `PlanWriterHost` still requires table-owned focus, busy, reread and notice callbacks. Those values are created inside `usePlanRead` after `WbsTable` mounts, while the accepted owner must sit above the header, saved-plan shelf and table.

050.7 therefore has a blocking first slice before adding the project owner:

1. Move the plan snapshot, connection/roster state, busy state and their update rules into project-owned plain stores. Replace React setters in `PlanFeedForReader` and `PlanWriterHost` with narrow project store/port interfaces. Move the command-currentness register and refusal publication behind those ports. React selects snapshots and invokes feature interfaces; it supplies no state setter to composition.
2. Preserve component-owned presentation state: focus location and focus requests remain delivery concerns. Replace `noteCommandIssued` with a narrow event/port the table subscribes to rather than injecting a React ref callback into the writer. The event says that a command was issued; the table decides how that affects focus.
3. Extract the broad `ProjectApi` behind the plan and command modules' private repository ports. The project composition root supplies those ports; `WbsTable` receives only project feature/store interfaces.
4. Add the production-path module/store tests for those changes, including the existing stale-owner, busy, refusal, roster and focus behavior. Only then can the ProjectPage-level effect create the complete project runtime without upward-threaded setters or a runtime hidden back inside `WbsTable`.

Saved plans and remaining command services are parallel extraction prerequisites described below. A project runtime slice that omits them must say so and must keep their existing keyed cleanup; it cannot claim the complete accepted lifetime is composed.

## Actual graph by accepted lifetime

Names below are responsibility names, not proposed runtime IDs.

| Lifetime    | Actual responsibility / status at this commit                              | Construction or adapter now                                                                                                                                                                 | Production callers now                                                                                    | Composition consequence already fixed by the design                                                                                                                                                                                                                            |
| ----------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Application | Auth gateway, not yet extracted                                            | `fetchMe`, auth calls, and `AuthForm` are wired in `app.tsx`; the startup session check is an effect in `AppContent`                                                                        | Signed-out gate and sign-in form                                                                          | Own the auth feature for the page. Session creation is driven by its authenticated user result.                                                                                                                                                                                |
| Application | Preferences, extracted                                                     | `modules/preferences/composition.ts` creates module-load `browserPreferences` over `browserStorage()` and the `rememberedPreferences` feature facade                                        | theme, last-opened project, Gantt/detail and layout preferences; `ProjectPage` imports the feature facade | Install once in the application runtime. Preserve the repository's dynamic access to `window.localStorage`; capturing a storage object at startup would break its tested boundary.                                                                                             |
| Application | Stream connector repository, not extracted as a DI module                  | `lib/project-stream.ts` owns browser `WebSocket`, timers, cancellation and randomness in `browserDeps`; `subscribeToProject` creates a per-project subscription                             | `ProjectPage` adapts it to the table; saved-plan shelf separately calls it                                | The browser connector belongs to the application runtime as a private repository. Each project subscription remains project-owned and must be disposed with that project. Components must not receive the repository through context.                                          |
| Application | Notices, not extracted                                                     | `useToasts()` is instantiated in both `ProjectPage` and `WbsTable`; refusal callbacks close over its API                                                                                    | project import/catalog, plan feed/writes/commands                                                         | The application context may expose only the notices feature contract. Existing local stacks show that extraction must first preserve which stack renders each message; a DI move cannot silently merge or duplicate announcements.                                             |
| Session     | Project catalog, not extracted                                             | `ProjectPage` memoizes `httpProjectApi(token)` and directly owns list/create/open/rename, selected ID, remembered selection and load errors                                                 | `ProjectPage` picker/import plumbing                                                                      | Runtime owns the catalog resource for one `session.user.id`. Delivery still contains catalog policy, so this is an extraction prerequisite rather than an existing service that can merely be registered. K2 requires a feature delivery facade before it reaches context.     |
| Session     | Directory, extracted resource plus feature facade                          | `directoryManagementFor/Over` composes `DirectoryManagement` over `Directory`; `useDirectoryManagement` builds it once per page mount and replaces its client                               | `DirectoryPage` through `modules/directory-management/view/use-directory-management.ts`                   | Install once per identity and expose `DirectoryManagement`, not `Directory` or `DirectoryApi`. Preserve `replaceClient` and its stable-snapshot behavior for the existing injected-client seam; do not promote it into a partial production credential-refresh mechanism.      |
| Project     | Plan feed, extracted resource plus feature facade                          | `planFeedForReader` creates `PlanRefresh` resource ownership and optional stream, wrapped by `PlanFeed`; `usePlanRead` effect creates it and cleanup clears `feedRef` before `feed.close()` | `usePlanRead`, then `WbsTable`; the stream also publishes roster into `ProjectPage`                       | Register the feed and its subscription in the project runtime. Preserve the active-reader predicate, clear-before-close behavior, one stream per table/project, baseline replay, and roster delivery. Expose the `PlanFeed` feature surface, never `PlanRefresh` or connector. |
| Project     | Plan writer, extracted feature                                             | `createPlanWriter` is currently memoized in `usePlanRead` over the current feed owner, reread callback, focus intent, busy setter and refusal announcer                                     | all table write gestures through `writer.run`                                                             | Install per project over the project feed and expose `PlanWriter`. Its current-owner checks must still stop old mutation success/refusal from affecting the replacement.                                                                                                       |
| Project     | Calendar marker commands, extracted exclusive resource plus feature facade | `calendarMarkersForReader` composes `CalendarMarkers` over its private marker resource and current feed owner                                                                               | four chart/table marker gestures                                                                          | One project module; expose `CalendarMarkers` only. The resource stays private, resolving K2 without putting marker HTTP methods or refresh ownership in delivery.                                                                                                              |
| Project     | Other command services, not extracted                                      | field, structure, dependency, estimate, reference and step command policy remains in WBS hooks/components and on the broad `ProjectApi`                                                     | `WbsTable` and its hooks                                                                                  | These are prerequisites for the final project runtime surface. Do not register the broad `ProjectApi` as a public context value; each extracted command feature consumes its narrow private resource/port.                                                                     |
| Project     | Saved plans, not extracted                                                 | `browserSavedPlansDeps()` creates list/save/compare and a second `subscribeToProject` path; `SavedPlansPanel` owns shelf/watch state                                                        | header or cards shelf keyed by selected project                                                           | Project runtime owns the saved-plan service and its watch. Delivery needs a feature facade before K2 can be satisfied; its subscription must close on project replacement.                                                                                                     |

Presentational and ephemeral React state does not become a runtime service: picker draft/highlight/hover, table focus and keyboard intent, viewport renderer, editor drafts, panel state and DOM refs remain with the components. The `key={selected}` on `WbsTable` is an existing assertion that each project owns its rows and transient editor state; DI must preserve that remount behavior.

## Ownership seams and replacement triggers

### Application owner

`main.tsx` is the page composition boundary. It currently creates the React root and renders `App` inside `<StrictMode>`. Build the application runtime at bootstrap outside the StrictMode subtree, resolve its narrow public services, and place only those services in the application provider. A plain host-level lifetime coordinator owns the current application, session and project handles; it is bootstrap wiring, not a fourth runtime or a service exposed through context. React owners tell it when identity/selection changes, and every close trigger joins the same in-flight retirement operation.

The accepted table says application closes on page hide, including a persisted `pagehide`. Therefore BFCache restoration is an implementation consequence, not a new product choice: page hide initiates project, then session, then application retirement and unmounts/invalidates the React root. A persisted `pageshow` joins that same retirement promise and invokes the complete bootstrap path only after it succeeds: build fresh runtimes/root/listeners, restore the signed-in identity through `fetchMe`, preserve the browser address, and let catalog/directory/feed perform their normal arrival reads. Retirement rejection or bounded-wait expiry publishes the sanitized fatal state and starts no bootstrap; eventual cleanup completion does not silently resume it. Non-persisted navigation does not rebuild. Since a browser lifecycle event cannot be relied on to await a promise, `pagehide` provides best-effort initiation rather than proof of completion; controlled tests await the coordinator promise.

A development edit that reaches the bootstrap is a **document replacement**, not a hot update: no module on the path to the page's entry accepts it, so Vite reloads the whole page. The old document's retirement is then initiated by its own `pagehide`, exactly as for any other navigation, and is not a gate: its completion is not observed, its failure cannot refuse the reload, and the new document bootstraps regardless. A **gated in-document replacement** — the replacement module bootstrapping inside the same live document only after the old runtime's retirement has succeeded, under the same terminal retirement gate as every other transition — is not provided. Whoever adds one inherits that gate and the races recorded in [the 050.7 e2 record](../2026-09-21-batch-6/050-7-e2-hmr-ownership.md). The OpenSpec change `adopt-frontend-lifetimes` states document replacement as a requirement and gated in-document replacement as not provided.

Hazards:

- building the application runtime in a component or lazy React initializer makes development StrictMode construct it twice;
- registering a resource by returning an object that happens to have `close()` does not dispose it; use DI Bag ownership explicitly;
- calling bounded close and treating timeout as cancellation leaks the still-running cleanup from the owner's accounting;
- a gated in-document replacement, if one is ever added, races page hide, so listener cleanup plus the coordinator's shared retirement promise must prevent a second graph from being created while the old owner still appears current;
- rebuilding only the bag on BFCache `pageshow` leaves React contexts pointing to closed services. Re-run the whole bootstrap/root publication path;
- the existing directory `visibilitychange`/focus arrival rereads and project reconnect behavior remain useful for ordinary tab backgrounding, which does not fire this terminal owner path.

### Session owner

`AppContent` has the only current production source of `Session`: successful password login hands it the declared response and startup `fetchMe` reconstructs it from the access cookie with `token: ''`. No mounted same-user credential refresh or rotation event exists today. `httpDirectoryApi(token)` and `httpProjectApi(token)` capture the header token in closures; the WebSocket and saved-plan clients use same-origin cookies. `DirectoryManagement.replaceClient` supports an injected-client change, but catalog/feed/commands have no corresponding atomic replacement contract.

Create/replace the session runtime on transitions of `session.user.id`, never on token or API object identity. For 050.7's present production behavior, its credential is immutable for that mounted session: a fresh login/startup identity creates a fresh session, and local exit retires its local runtime without revoking the credential. Do not invent a partial token-rotation path that updates directory while leaving catalog/project services on the old client. If a same-ID credential refresh source is later added, that behavior requires its own contract/change: the application auth feature publishes one credential generation, the coordinator retires the current project before atomically replacing every header-token client (directory and project catalog plus every project repository), then recreates the project for the same selected ID. Cookie-backed saved-plan/socket adapters need no token swap but still participate in project retirement. The accepted identity rule means the session stores may survive that operation; it does not make today's absent refresh event real.

The existing `AccountMenu` “Log out” action is a **local exit** in 050.7. Today it only calls `setSession(null)`; no frontend caller invokes the declared `POST /api/auth/logout`, so neither the server refresh state nor cookie is authoritatively revoked and a reload may restore the same identity. Preserve that observable behavior in this scope rather than adding remote revocation implicitly.

Local exit calls the host coordinator before clearing local state: the coordinator invalidates project publication, joins/closes the project, invalidates session publication, joins/closes the session, and only after successful retirement commits the local signed-out UI state. A retirement failure or timeout refuses that local transition and publishes the sanitized fatal/public-report state described below. It never republishes the withdrawn old project/session services, even though the still-valid remote cookie may authenticate a later full reload. Project selection effects likewise call the coordinator, so route unmount, switch, local exit and page hide — which is also how a document replacement retires the page — converge on the same current handle and promise. This supplies the project-before-session control path that independent nested effect cleanup cannot guarantee. Authoritative logout is separate observable auth work and remains outside 050.7.

Keep the router instance stable. `AppRouter` intentionally creates it once and refreshes its context because recreating it loses the current address. Router context may carry the narrow session delivery services required by lazy routes plus the existing presentational `account`, `presence`, and `nav`; it must no longer carry `token`, `ProjectApi`, `DirectoryApi`, or a bag once extraction is complete.

Hazards:

- using username as identity breaks on rename; use the existing user ID;
- a future credential source that updates only `DirectoryManagement.replaceClient` splits one session across old and new credentials;
- an identity change that only swaps clients retains the previous user's stable snapshot;
- the current `useDirectoryManagement` lazy state initializer is safe only because its service has no disposal. A disposable runtime created there would be closed by StrictMode's first cleanup and reused by its second setup.

Update, observed 2026-09-24 (050.7i, OpenSpec task 6): the session owner is `SignedInApp`'s, in `apps/wbs/fe-01/src/app.tsx`, built over one lifetime slot by `createSessionOwner` in `apps/wbs/fe-01/src/runtime/session-runtime.ts` and keyed by `session.user.id`: an identity for the user already opened replaces nothing, so the router keeps its instance, its address and its mounted route. Each session runtime installs the sealed directory-management module over `httpDirectoryApi(credential)` — the credential is only that adapter's input — and owns the session's project owner, which `ProjectPage` now receives through router context instead of building its own. Withdrawing a session withdraws its project in the same instant; its retirement leaves the project first and fails when the project cannot be given back, so Log out (task 7) can be one `leave()` of the session owner. The project catalog, the archival import and the header token still reach `ProjectPage` directly: the catalog facade remains a prerequisite.

### Project owner

`ProjectPage.selected` is the project identity. Selection currently remounts `WbsTable` with `key={selected}` and separately keys the saved-plan shelf. After the blocking store/port extraction above, the project owner must encompass every project consumer: header roster, saved-plan shelf, and table. Its effect asks the host coordinator for a fresh runtime for each non-null selected ID within the current session generation, publishes narrow services only while that runtime is current, and clears publication before joining close. API object identity is not a project key. Null selection, route change to `/directory`, project switch, credential-generation replacement, session replacement/local exit, page shutdown, and owner unmount all call the same coordinator retirement operation.

The current `usePlanRead` effect is the behavioral oracle: one project/API/feed/stream; current identity checked before publication; `feedRef.current` cleared before `feed.close()`; stale in-flight writes/refusals do not spend against a replacement. Move this ownership, do not weaken it. The current stream joins feed invalidation and roster on one socket; do not accidentally restore a second presence socket. Saved plans currently has its own subscription and must remain independently owned unless a later accepted design changes that behavior.

Hazards:

- allocating a project runtime during render or in `useMemo` leaks abandoned renders and fails the StrictMode setup/cleanup/setup cycle;
- closing after publishing the replacement without first invalidating the old owner lets late work target the new runtime;
- keying replacement on an API object rebuilt each render causes reconnect storms; current `ProjectPage` memoizes the client by token/override;
- a project provider around only `WbsTable` strands the roster and saved-plan shelf outside their owner;
- awaiting an unbounded close before installing the replacement can leave the selected page permanently blank on a stuck disposer. The packet needs an explicit, tested transition policy using DI Bag's bounded-wait semantics, without claiming that a timed-out cleanup stopped.

Update, observed 2026-09-24 (050.7j, OpenSpec task 10, not yet closed): the project owner is `ProjectPage`'s, built over one lifetime slot by `createProjectOwner` in `apps/wbs/fe-01/src/runtime/project-runtime.ts`, above the header and the table. Its effect opens a DI Bag project runtime per selected project — delivered plan, busy, presence, both channels, feed, marker gestures, writer and commands — withdraws it synchronously on a switch, an unmount or Strict Mode's re-entry, and retires it once; a failed retirement shows the sanitized fatal state in place of the page's main. The table receives `ProjectRuntime` and opens nothing. Presence now resets on a switch. Saved plans are not yet in the runtime: the shelf keeps its own keyed watch, and the saved-plans prerequisite above stands.

## Narrow context and K2 resolution

Contexts contain readonly service interfaces, never construction inputs or infrastructure.

| Context boundary                                  | Public values justified by current callers                                                                           | Values kept private                                                                                |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| Application, above `App`                          | auth feature, `RememberedPreferences`, notices feature                                                               | DI bag, browser storage, WebSocket/timer/random connector                                          |
| Session, around the signed-in `AppRouter`         | directory-management feature; eventual project-catalog delivery feature; presentation slots may remain router values | token, broad HTTP clients, directory resource, repository adapters                                 |
| Project, around the whole selected-project region | `PlanFeed`, `PlanWriter`, `CalendarMarkers`, eventual saved-plans and remaining command feature surfaces             | refresh owner/resource, marker resource, broad `ProjectApi`, socket connector, saved-plan adapters |

Existing K2 resolutions that should be retained:

- `RememberedPreferences` is the delivery facade over the private preferences resource/repository. The generic non-React `lib/remembered.ts` compatibility factory is not a reason to expose that resource in React context.
- `DirectoryManagement` is the delivery facade over `Directory`; the current composition file is the only place that sees both.
- `PlanFeed` wraps the plan refresh resource; delivery should subscribe/select through it rather than receive `PlanRefresh`.
- `CalendarMarkers` is the feature facade for its exclusive private resource.
- `PlanWriter` is already a feature interface.
- `PlanCommands` is the delivery facade over the plan commands' private `PlanCommandRoutes`, and `ProjectServices` hands the table the feed, the marker gestures and the commands as factories of feature-services, over ports the project composition root cuts from one client (050.7h, OpenSpec task 9).

Two gaps must be handled as extraction work, not bypassed by context: project catalog and saved plans currently have no feature facade, while React delivery directly coordinates their resource/API behavior. The broad `ProjectApi` also serves unfinished command extractions. Passing any of those through a new context would preserve the present K2 violation behind a provider.

Update, observed 2026-09-24 (050.7h, OpenSpec task 9): the broad `ProjectApi` no longer serves the command extractions or any plan module. `ProjectPage` holds it for the project catalog and the archival import, which are the session's, and hands it to `projectServicesOver` (`apps/wbs/fe-01/src/modules/project/composition.ts`) only; the table, its hooks, its toolbar and its columns receive `ProjectServices` and `PlanCommands`. The command policy itself still lives in the table's hooks — the command services' extraction, each over a narrower port, remains a prerequisite of the final project surface.

## Replacement and cleanup policy

Routine consequences already fixed by the accepted design:

- withdraw the old context/current-owner reference synchronously before starting its close;
- project retirement always begins before session retirement; application retirement begins only after both child retirements have been started/joined;
- all triggers for the same current handle join one close promise;
- every close rejection and every bounded-wait rejection is observed through one non-React lifecycle-failure reporting boundary that uses the same sanitized public/diagnostic reporting and occurrence correlation as the root fault path. It must not call React's root callbacks as if cleanup were a render fault, and it must never log the thrown value;
- disposer failure does not stop DI Bag from attempting the other disposers, and timeout does not cancel cleanup. The coordinator retains and observes the shared shutdown promise after a bounded wait rejects.
- required retirement is a transition precondition. After withdrawing the old publication, the coordinator publishes a replacement or completes local exit only when that retirement succeeds. Aggregate cleanup failure or bounded-wait expiry fails the transition, refuses the replacement, and exposes a visible fatal state containing only the sanitized public report and correlation handle. It never reports and continues into overlapping ownership;
- after timeout, the coordinator keeps the failed transition closed and retains observation of the shared shutdown promise. Eventual cleanup completion does not silently resume or publish the refused replacement; the existing fatal-state reload action may start a fresh page bootstrap.

The finite close budget is a routine implementation choice, not a user decision or permission to overlap runtimes. The implementation packet names one deterministic value (and any justified lifetime-specific difference) and proves it with an injected never-settling disposer. The only known pending user preference in this map remains the runtime identifier grammar.

## Exact lifecycle tests for the implementation packet

These are focused production-wiring tests. Each safety assertion needs its R5 mutation and adjacent `Proof:` note when implemented.

1. **Application bootstrap is outside StrictMode.** Render the real bootstrap composition with a recording application module under `<StrictMode>`. Assert one application runtime and one owned application resource are acquired, not two. Mutation: move runtime creation under the StrictMode component; observe acquisition count 2.
2. **Application shutdown under a document replacement is page hide's.** A development edit to the bootstrap reloads the document, so there is no `import.meta.hot.dispose` callback to capture: the old document's shutdown is its `pagehide`. Dispatch a non-persisted `pagehide` on a live page whose disposal is still pending and, before any await, assert the root is taken down, publication is withdrawn, disposal has begun and a preference write through a captured handle throws. Mutation: defer the retirement past the dispatch; the runtime is still published when the dispatch returns. A gated in-document replacement, if one is ever added, needs this test's original form — both callbacks fired in each order, one shutdown, listener removal — as well.
3. **Persisted page restoration joins retirement before rebuilding.** Start at `/directory`, dispatch a persisted `pagehide`, leave one disposer deferred, then dispatch `pageshow` with `persisted: true`. Assert no application runtime, React root, listener, identity read or project/session service is rebuilt while retirement is pending. Settle retirement successfully and assert the complete bootstrap runs once, `fetchMe` restores the session, the browser address remains `/directory`, and directory performs its normal arrival read. Repeat from an open project and assert a fresh feed/socket rather than reuse of the closed feed. In rejection and budget-expiry variants, assert the sanitized fatal state, zero bootstrap attempts, and continued observation of late cleanup. Mutation: start bootstrap directly from `pageshow`; the replacement is observed before retirement settles. Mutation: rebuild only the bag without republishing the root; context still exposes the old service.
4. **Application disposal failure is visible.** Make one owned disposer reject while another records completion. Assert shutdown rejects with the DI Bag cleanup failure, the other disposer still ran, and the sanitized lifecycle-failure reporter receives exactly one correlated occurrence. Mutation: swallow the close promise; the reporter receives nothing. Page-hide tests assert initiation/reporting, not browser waiting.
5. **Session identity and the actual credential source.** Exercise both real installation paths: startup `fetchMe` installs the returned user with `token: ''`, and password login installs the declared session response. Assert both pass `user.id` as the coordinator key and the session credential only as adapter input. In the coordinator's focused contract test, feed declared sessions `u1/''`, `u1/'t'`, then `u2/''`: the same-ID input does not replace the runtime (there is no production event that emits it today), while the ID change closes the first session and exposes no `u1` directory/catalog snapshot. Compile/production wiring assertions enumerate consumers: `httpDirectoryApi` and `httpProjectApi` receive the session credential, while saved plans and WebSocket remain cookie-backed. Mutation: key the owner on token; the focused contract replaces `u1` and collides `u1`/`u2` on `''`, while the production-path assertions prove both token shapes reach that key function. Mutation: retain stores on ID change; `u1` state appears for `u2`.
6. **The Log out action remains a coordinated local exit.** Start a session and selected project through real `AppContent`/`AccountMenu` wiring, activate Log out, and assert zero `/api/auth/logout` requests, publication withdrawal, project stream/feed/saved-plan close, then session directory/catalog close, each once, before local signed-out UI renders. On a simulated reload, allow `fetchMe` to return the still-cookie-authenticated identity and assert the session can be restored. Trigger route cleanup concurrently and assert both callers join the same project close. In the failure variant, reject project or session close and assert neither normal signed-out UI nor the old local services are republished; the sanitized fatal/public-report state does. Mutation: restore direct `setSession(null)` or remove project retirement; signed-out UI appears while the socket/watch remains live. Mutation: re-expose the retired session after close failure; the old context becomes readable instead of remaining fatal. A future authoritative logout change needs separate OpenSpec and request/revocation proofs.
7. **Router address survives same-session updates.** Enter `/directory` using the real router and update a presentation value for the same user. Assert pathname and router instance remain. Existing `app-router.test.tsx` and `app.test.tsx` address tests are the regression oracle. Mutation: key/recreate `AppRouter` from session context; address or mounted route state resets.
8. **Project switch replaces all project ownership.** Select `p1`, establish its feed socket, roster and saved-plan watch, then select `p2`. Assert `p1` context publication is withdrawn before its disposers run; its feed socket and saved-plan watch close once; only then is `p2` published with fresh feed/writer/commands/shelf and transient table state. Mutation: omit project ID from the owner effect; `p1` remains current. Mutation: close only feed; old saved-plan watch remains subscribed.
9. **Project route unmount, session persistence.** With a directory snapshot and `p1` open, navigate to `/directory`. Assert `p1` closes once and directory/catalog session identities remain unchanged. Navigate back and assert a new project runtime is created. Mutation: place the session owner below the route; directory/catalog are recreated.
10. **StrictMode creates a live second project runtime.** Wrap the real project owner in `<StrictMode>`, open one project, and assert setup 1 creates runtime/stream 1, cleanup closes 1, setup 2 creates distinct runtime/stream 2, and unmount closes 2. Assert no service from runtime 1 is published after cleanup. This extends the existing `plan-read-and-write.test.tsx` case `creates a live second owner after StrictMode cleans up its first setup`. Mutation: create runtime in `useState`/`useMemo`; setup 2 receives a closed runtime.
11. **Old project completions cannot publish.** Defer p1 read, mutation success and refusal; switch to p2; settle each p1 promise. Assert p2 snapshot, busy state, roster and notices are unchanged. Existing refresh-owner cases in `plan-read-and-write.test.tsx` are the behavior oracle. Mutation: remove the current runtime/identity predicate; a deferred p1 outcome changes p2.
12. **Project cleanup failure refuses replacement.** Reject one p1 disposer while another completes during p1→p2. Assert all p1 cleanup is attempted, one sanitized correlated failure reaches the lifecycle-failure reporter, no unhandled rejection occurs, p1 remains withdrawn, p2 is neither created nor published, and the fatal/public-report state is visible. Mutation: continue by publishing p2 after the rejection; the negative observes overlapping/continued ownership instead of the fatal state. Mutation: `void runtime.close()` without rejection observation; reporter count is zero or an unhandled rejection is observed.
13. **Session cleanup failure is reported after project retirement and refuses continuation.** Reject the session directory/catalog disposer during local exit and during u1→u2. Assert project cleanup was already attempted, remaining session disposers run, one sanitized correlated report is emitted, no old service remains published, and neither local signed-out normal UI nor the u2 session is published; the fatal/public-report state is visible. Mutation: continue to the requested state after the rejection; the fatal state is absent. Mutation: reject early from the coordinator without observing the shared close promise; later cleanup/failure is lost.
14. **Close timeout fails the transition while cleanup continues.** Give a project disposer a never-settling close, request p2, advance the deterministic close budget, and assert the timeout is reported with the pending disposer, p1 remains withdrawn, p2 is neither created nor published, and the fatal/public-report state is visible. Then allow late cleanup to settle and assert its completion remains observed but does not silently publish p2. Mutation: treat timeout as cancellation/success and publish p2; the replacement appears while required retirement is unresolved. Mutation: drop the shared shutdown promise after timeout; completion/error becomes unobservable.
15. **No infrastructure escapes contexts.** Add compile-time fixture checks for each provider's exported context type and production-path architecture checks: no `Bag`, token string, broad `ProjectApi`/`DirectoryApi`, repository or resource-service is exposed/imported by React delivery. Break each by exporting/importing the forbidden value and observe the named check fail. The DI graph/module checks separately prove every required public service resolves.

Focused test homes supported by current boundaries are `main.test.tsx`/`index-bootstrap.test.ts` for application ownership, `app.test.tsx` and `app-router.test.tsx` for session identity/router behavior, `project-page.test.tsx` for selection and route ownership, `directory-page.test.tsx` for directory continuity, and `plan-read-and-write.test.tsx` plus module tests for feed/write/command stale-owner behavior. Do not replace the existing tests: they are the before/after behavioral oracle.

## Planning constraints and open implementation facts

- Runtime identifiers are intentionally absent pending the user's grammar choice.
- This commit does not yet contain DI Bag modules or lifetime owners, so exact exported builder names and module IDs belong to the later packet after that decision.
- The package supplies runtime primitives but no installed React integration source. The official `v0.4.0` tag supplies a tested copyable recipe, not exported API; implement and test the host owner locally and keep R5's stricter failure gate.
- Project catalog, saved plans, notices/auth, stream connector, and most plan commands are not extracted at this commit. 050.7 must order those prerequisites or explicitly limit its first composition slice; it cannot truthfully claim the complete accepted lifetime map is installable today.

Primary-source lifecycle research: [DI Bag 0.4.0 React lifecycle guarantees](../../../research/2026-09-21-di-bag-0-4-lifecycle.md).
