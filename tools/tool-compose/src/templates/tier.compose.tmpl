services:
  {{CONTAINER}}:
    image: {{IMAGE}}
    container_name: {{CONTAINER}}
    restart: unless-stopped
    networks:
      {{NETWORK}}:
        aliases:
          - {{CONTAINER}}
{{ENV_FILES}}{{VOLUMES}}
