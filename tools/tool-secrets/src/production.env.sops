# Placeholder — replace with real SOPS-encrypted contents via:
#   sops --encrypt --age $RECIPIENT tools/tool-secrets/src/production.env > \
#        tools/tool-secrets/src/production.env.sops
# This file is intentionally empty in-repo so the scaffold doesn't leak
# plaintext or dummy encrypted material.
