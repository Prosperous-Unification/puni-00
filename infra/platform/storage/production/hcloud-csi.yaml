apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hcloud-csi
  namespace: kube-system
spec:
  interval: 30m
  dependsOn:
    - name: hcloud-ccm
  chart:
    spec:
      chart: hcloud-csi
      version: 2.23.0
      sourceRef:
        kind: HelmRepository
        name: hcloud
        namespace: flux-system
