---
- name: Exercise the production enrollment identity assertion
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Run the shared production assertion
      ansible.builtin.include_tasks: ../tasks/require-enrollment-identity.yml
