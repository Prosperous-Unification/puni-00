---
- name: Configure and bootstrap each designated first k3s server
  hosts: k3s_bootstrap_servers
  become: true
  gather_facts: true
  serial: 1
  roles:
    - role: base
    - role: network
    - role: storage
    - role: k3s_server
      puni_k3s_cluster_init: true
